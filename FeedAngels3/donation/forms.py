from django import forms
from django.forms import DateField

from .models import donateFood
""""
class donateFoodForm(forms.ModelForm):
    class Meta:
        model = donateFood
        fields = ['district', 'address', 'description', 'expiration_date']
"""
